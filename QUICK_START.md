# 🚀 Quick Start Guide

## Установка и запуск

### 1. Установите зависимости

```bash
npm install
```

### 2. Запустите dev-сервер

```bash
npm run dev
```

Сайт откроется автоматически в браузере на `http://localhost:3000`

### 3. Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### 4. Просмотр production сборки

```bash
npm run preview
```

## 📁 Структура проекта

### Стили (SCSS)
- `src/styles/base/` - базовые стили (переменные, reset, типографика, анимации)
- `src/styles/components/` - компоненты (hero, buttons, cards, timeline, marquee, sections)
- `src/styles/layout/` - макет (main, footer)
- `src/styles/utilities/` - утилиты (responsive mixins)
- `src/styles/main.scss` - главный файл, импортирующий все модули

### JavaScript
- `src/js/main.js` - точка входа
- `src/js/modules/` - модули функциональности:
  - `smoothScroll.js` - плавная прокрутка
  - `marquee.js` - анимация marquee
  - `animations.js` - анимации при скролле
  - `navigation.js` - навигация и scroll indicator
- `src/js/utils/` - утилиты (helpers.js)

### HTML
- `src/index.html` - главный HTML файл

## 🔧 Команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера с HMR |
| `npm run build` | Сборка для production |
| `npm run preview` | Просмотр production сборки |
| `npm run lint` | Проверка кода (если настроен ESLint) |

## 📝 Изменение стилей

Все стили находятся в `src/styles/`. Для изменения:

1. **Цвета и переменные**: `src/styles/base/_variables.scss`
2. **Компоненты**: `src/styles/components/`
3. **Адаптивность**: `src/styles/utilities/_responsive.scss`

## 🎨 Добавление новых компонентов

1. Создайте файл в `src/styles/components/_your-component.scss`
2. Импортируйте в `src/styles/main.scss`:
   ```scss
   @import 'components/your-component';
   ```

## ⚡ Производительность

- Vite автоматически оптимизирует код
- CSS минифицируется в production
- JavaScript модули разделяются автоматически
- Tree-shaking удаляет неиспользуемый код

## 🐛 Отладка

- Dev-сервер поддерживает Hot Module Replacement (HMR)
- Изменения в SCSS и JS применяются мгновенно
- Используйте DevTools браузера для отладки

## 📦 Деплой

### GitHub Pages

1. Соберите проект: `npm run build`
2. Настройте GitHub Pages на папку `dist/`
3. Или используйте GitHub Actions для автоматического деплоя

### Vercel / Netlify

Просто подключите репозиторий - они автоматически определят Vite проект.

